<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Row 1: Timer Display + Theme Toggle (separate box) -->
    <LinearLayout
        android:id="@+id/widget_timer_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="8dp"
        android:background="@drawable/widget_section_dark"
        android:layout_marginBottom="4dp">

        <!-- Timer Display -->
        <Chronometer
            android:id="@+id/widget_timer_chronometer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="26sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />

        <!-- Theme Toggle (right corner) -->
        <TextView
            android:id="@+id/widget_btn_theme"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:text="◐"
            android:textSize="14sp"
            android:textColor="#E2E8F0"
            android:gravity="center"
            android:background="@drawable/widget_btn_theme" />

    </LinearLayout>

    <!-- Row 2: Timer Controls (separate box) -->
    <LinearLayout
        android:id="@+id/widget_controls_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="6dp"
        android:background="@drawable/widget_section_dark"
        android:layout_marginBottom="6dp">

        <TextView
            android:id="@+id/widget_btn_play"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:text="▶"
            android:textSize="13sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:background="@drawable/widget_btn_rounded" />

        <TextView
            android:id="@+id/widget_btn_pause"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:text="❚❚"
            android:textSize="10sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:layout_marginStart="6dp"
            android:background="@drawable/widget_btn_rounded" />

        <TextView
            android:id="@+id/widget_btn_reset"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:text="↺"
            android:textSize="13sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:layout_marginStart="6dp"
            android:background="@drawable/widget_btn_rounded" />

    </LinearLayout>

    <!-- Subject List -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="3dp"
        android:scrollbars="none"
        android:fadingEdge="vertical" />

    <!-- Empty State -->
    <TextView
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="No subjects"
        android:textColor="#64748B"
        android:textSize="12sp"
        android:visibility="gone" />

</LinearLayout>
